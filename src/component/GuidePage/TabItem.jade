
- const highlightWhenActive = (current === target)? Style.tab.Active: {};

div(style=[ Style.tab.item, highlightWhenActive ], onClick=update(target))
  span(style=[ Style.tab.icon ])
    = React.createElement(Icon[target])
  p(style=[ Style.tab.name ])= name
